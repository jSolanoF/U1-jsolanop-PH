import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.u1.jsolano',
  appName: 'ph-u1-jsolano',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
